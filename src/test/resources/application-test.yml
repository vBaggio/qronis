spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: true

jwt:
  secret: qronis-test-secret-com-pelo-menos-32-caracteres
  expiration-hours: 1
  issuer: qronis-test

cors:
  allowed-origins:
    - "http://localhost:5173"
    - "http://127.0.0.1:5173"

logging:
  level:
    org.springframework.security: DEBUG
    com.qronis: DEBUG
