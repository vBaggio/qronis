spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: true

jwt:
  secret: qronis-test-secret-com-pelo-menos-32-caracteres
  expiration-hours: 1
  issuer: qronis-test

logging:
  level:
    org.springframework.security: DEBUG
    com.qronis: DEBUG
